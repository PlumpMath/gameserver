package co.selim.gameserver.model;

public class GameMap {
    private Tile[][] tiles;

    public GameMap() {
        this.tiles = new Tile[48][48];
        for(int x = 0; x < tiles.length; x++) {
            for(int y = 0; y < tiles[x].length; y++) {
                
            }
        }
    }
}
